export const MAX_ROWS = 3

// In some cases one activity is built from two history rows, so we fetch double the amount of rows to ensure we have enough to display.
export const HISTORY_FETCH_LIMIT = MAX_ROWS * 2
