// Desktop-only card surface (flat on mobile), matching SurfaceCard's look — shared
// by the swap / earn deposit / earn withdraw flows so they read as a card on md+
// but stay edge-to-edge on phones.
export const DESKTOP_CARD = 'md:rounded-3xl md:bg-white md:p-6 md:dark:bg-card md:shadow-[var(--card-shadow)]'
